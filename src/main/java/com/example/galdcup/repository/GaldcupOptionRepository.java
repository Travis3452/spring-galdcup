package com.example.galdcup.repository;

import com.example.galdcup.entity.GaldcupOption;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GaldcupOptionRepository extends JpaRepository<GaldcupOption, Long> {
}
