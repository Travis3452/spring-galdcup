package com.example.galdcup.repository;

import com.example.galdcup.entity.GaldcupTopic;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GaldcupTopicRepository extends JpaRepository<GaldcupTopic, Long> {
}
