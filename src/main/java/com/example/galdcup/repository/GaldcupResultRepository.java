package com.example.galdcup.repository;

import com.example.galdcup.entity.GaldcupResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GaldcupResultRepository extends JpaRepository<GaldcupResult, Long> {
}
